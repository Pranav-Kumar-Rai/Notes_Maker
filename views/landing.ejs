<!DOCTYPE html>
<html>
  <head>
    <title>Notes_Maker</title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="stylesheets/landing.css" />
    <script
      type="text/javascript"
      src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.js"
      async
    ></script>
  </head>

  <body>
    <div class="container">
      <% if (error &error.length > 0) { %>
      <div class="btn btn-danger" role="alert"><%= error %></div>
      <% } %> <% if (success &success.length > 0) { %>
      <div class="btn btn-success" role="alert"><%= success %></div>
      <% } %>
    </div>

    <div id="landing-header">
      <h1>
        Notes_Maker
        <br />
        <span id="tagline">A simple platform to organise all your notes<span>
      </h1>
      <a href="/notes" class="btn btn-lg btn-success">View Notes</a>
    </div>

    <ul class="slideshow">
      <li></li>
      <li></li>
      <li></li>
      <li></li>
      <li></li>
    </ul>
  </body>
</html>
